<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Marks</title>
    <style>
          @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
        body{
            font-family: 'Poppins', sans-serif;
            background:url("https://images.shiksha.com/mediadata/images/1606461255php4NobaM.jpeg") ;
            background-size: cover;
        }
        
        .container {
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 4px 4px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            width: 30vw;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%,-50%);
        }
        form{
            display: flex;
            flex-direction: column;
        }
        select,input,button{
            padding: 10px 14px;
            border: 1px solid rgb(104, 119, 104);
            font-family: 'Poppins', sans-serif;
        }
        button{
            border: transparent;
            background-color: rgb(85, 180, 85);
            color: white;
            cursor: pointer;
            
        }
        
    </style>
</head>
<body>
    <div class="container">
        <h1>Update Marks</h1>

        <% if (usn && usn.length > 0) { %>
            <form action="/submitmarks" method="POST">
                <label for="usn">Select USN:</label>
                <select name="usn" id="usn" required>
                    <% usn.forEach(record => { %>
                        <option value="<%= record.usn %>"><%= record.usn %> </option>
                    <% }); %>
                </select>
                <br><br>
    
                <label for="test">Select Test:</label>
                <select name="test" id="test" required>
                    <option value="test1">Test 1</option>
                    <option value="test2">Test 2</option>
                    <option value="test3">Test 3</option>
                </select>
                <br><br>
    
                <label for="marks">Enter Marks (out of 20):</label>
                <input type="number" id="marks" name="marks" min="0" max="20" required>
                <br><br>
    
                <button type="submit">Submit Marks</button>
            </form>
        <% } else { %>
            <p>No Record found.</p>
        <% } %>
    </div>
   
    
</body>
</html>
