<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Notes</title>
    <style>
        /* Global Styles */
        body {
            font-family: 'Poppins', sans-serif; /* Use Google Font Poppins */
            background-color: #f5f5f5; /* Light gray background */
            color: #333; /* Dark gray text color */
            margin: 0;
            padding: 0;
        }
        h1, h2 {
            color: #1a73e8; /* Google Blue */
            text-align: center;
            margin-bottom: 20px;
        }
        form {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff; /* White background */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        input[type="text"], input[type="file"], button {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ddd; /* Light gray border */
            border-radius: 4px;
            box-sizing: border-box;
            font-size: 16px;
        }
        input[type="file"] {
            padding-top: 15px; /* Adjust for file input */
        }
        button {
            background-color: #1a73e8; /* Google Blue */
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        button:hover {
            background-color: #0d47a1; /* Darker shade on hover */
        }
        hr {
            border: none;
            border-top: 1px solid #ddd; /* Light gray line */
            margin: 20px 0;
        }
        ul {
            list-style-type: none;
            padding: 0;
        }
        li {
            margin-bottom: 15px;
            padding: 15px;
            background-color: #fff; /* White background */
            border-radius: 4px;
            box-shadow: 0 0 5px rgba(0,0,0,0.1);
        }
        li:last-child {
            margin-bottom: 0;
        }
        strong {
            font-weight: bold;
        }
    </style>
</head>
<body>
    <h1>Upload Assignment</h1>

    <!-- Form for uploading assignment -->
    <form method="post" action="/add_assignment" enctype="multipart/form-data">
        <input type="text" placeholder="Enter assignment Name" name="assignment" required><br><br>
        <input type="file" name="notesFile" accept=".pdf,.doc,.docx,.txt" required><br><br>
        <button type="submit">Submit</button>
    </form>

    <hr>

    <!-- Display uploaded assignments -->
    <h2>Uploaded Assignments</h2>
    <ul>
        <% assign.forEach(assignment => { %>
            <li>
                <strong>Teacher Name:</strong> <%= assignment.teacher_name %><br>
                <strong>Assignment</strong> <%= assignment.assignment %><br>
             
            </li>
        <% }); %>
    </ul>
</body>
</html>
